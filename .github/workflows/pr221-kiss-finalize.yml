name: TEMP PR221 KISS Finalize
on:
  push:
    branches:
      - hobbyroom/107007-r001-kiss-core-20260910-v2
permissions:
  contents: write
jobs:
  build-test-finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
          persist-credentials: true
      - name: Apply minimal existing-package binding
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import hashlib, json

          ROOT=Path('.')
          ACTION=ROOT/'control/single-door-boundary/codex_current_action.py'
          HANDOFF=ROOT/'control/startmaster0107/fachworkflow_proof_handoff.py'
          STEP=ROOT/'control/startmaster0107/STEP_107007_RUN_NEW_ARTICLE_BATCH_NO_STOP.json'
          STATE=ROOT/'control/startmaster0107/CURRENT_STATE.json'
          START=ROOT/'control/startmaster0107/PFERDE_ATELIER_START_HERE.json'
          TEST=ROOT/'control/startmaster0107/test_fachworkflow_proof_handoff.py'

          def sh(p): return hashlib.sha256(p.read_bytes()).hexdigest()
          def dump(p,o): p.write_text(json.dumps(o,ensure_ascii=False,indent=2)+'\n',encoding='utf-8')
          def replace_block(text,start_marker,end_marker,new):
              a=text.index(start_marker); b=text.index(end_marker,a)
              return text[:a]+new.rstrip()+'\n\n'+text[b:]

          a=ACTION.read_text(encoding='utf-8')
          new_runtime=r'''def _runtime_context()->dict:
              runtime=load(REPO/RUNTIME_STATE_REL); batch=str(runtime.get('batch_sha256') or ''); pref=str(runtime.get('production_package_ref') or ''); digest=str(runtime.get('production_package_sha256') or '')
              generation=runtime.get('generation')
              if runtime.get('status')!='EXECUTION_READY' or runtime.get('publish_allowed') is not False or not re.fullmatch(r'[0-9a-f]{64}',batch): raise ViewError('RUNTIME_BATCH_BINDING_INVALID')
              if not isinstance(generation,int) or generation<1: raise ViewError('RUNTIME_GENERATION_INVALID')
              gbase=f'control/startmaster0107/runtime_inbox/generations/{generation:06d}'
              if pref!=gbase+'/PRODUCTION_PACKAGE.json': raise ViewError('RUNTIME_GENERATION_PACKAGE_REF_MISMATCH')
              pp=safe(pref)
              if not pp.is_file() or not re.fullmatch(r'[0-9a-f]{64}',digest) or sha(pp)!=digest: raise ViewError('RUNTIME_PRODUCTION_PACKAGE_BINDING_INVALID')
              pkg=load(pp)
              if pkg.get('contract')!='PSERC_APPROVED_PRODUCTION_PACKAGE_V1': raise ViewError('RUNTIME_PRODUCTION_PACKAGE_CONTRACT_INVALID')
              wr=pkg.get('workflow_release'); items=wr.get('items') if isinstance(wr,dict) else None
              if not isinstance(wr,dict) or wr.get('contract')!='WORKFLOW_SUPERVISOR_RELEASE_V2_SIGNED' or wr.get('status')!='PASS' or not isinstance(items,list) or not items: raise ViewError('RUNTIME_PRODUCTION_PACKAGE_CONTEXT_INVALID')
              if wr.get('exact_five_batch_sha256')!=batch or int(wr.get('exact_five_item_count') or -1)!=len(items) or wr.get('wordpress_write_performed') is not False: raise ViewError('RUNTIME_PRODUCTION_PACKAGE_IDENTITY_INVALID')
              sref=str(runtime.get('source_snapshot_ref') or ''); sdigest=str(runtime.get('source_snapshot_sha256') or '')
              if sref!=gbase+'/SOURCE_SNAPSHOT.json': raise ViewError('RUNTIME_GENERATION_SOURCE_REF_MISMATCH')
              sp=safe(sref)
              if not sp.is_file() or not re.fullmatch(r'[0-9a-f]{64}',sdigest) or sha(sp)!=sdigest: raise ViewError('RUNTIME_SOURCE_SNAPSHOT_BINDING_INVALID')
              snap=load(sp); mb=snap.get('next_textmachine_metadata_batch'); metas=mb.get('items') if isinstance(mb,dict) else None
              if not isinstance(mb,dict) or mb.get('batch_sha256')!=batch or not isinstance(metas,list) or len(metas)!=len(items): raise ViewError('RUNTIME_METADATA_BATCH_INVALID')
              plan=pkg.get('production_plan'); pitems=plan.get('items') if isinstance(plan,dict) else None
              if not isinstance(plan,dict) or plan.get('contract') not in {'production_plan_v4','production_plan_v5'} or not isinstance(pitems,list): raise ViewError('RUNTIME_PRODUCTION_PLAN_INVALID')
              bundle=pkg.get('fact_pack_bundle'); facts=bundle.get('fact_packs') if isinstance(bundle,dict) else None
              if not isinstance(bundle,dict) or bundle.get('contract')!='canonical_fact_pack_import_v1' or not isinstance(facts,list): raise ViewError('RUNTIME_FACT_PACK_BUNDLE_INVALID')
              header=dict(plan); header.pop('items',None); metadata=dict(wr); metadata.pop('items',None)
              return {'runtime':runtime,'batch':batch,'count':len(items),'package':pkg,'release_items':items,'meta_items':metas,'plan_items':pitems,'fact_packs':facts,'plan_header':header,'release_metadata':metadata,'source_sha256':sdigest,'package_sha256':digest}
          '''
          a=replace_block(a,'def _runtime_context()->dict:','def _runtime_batch_identity',new_runtime)
          new_bound=r'''def _bound_expected(it:Mapping[str,Any])->dict:
              c=_runtime_context(); slot=str(it.get('plan_slot') or ''); cid=str(it.get('canonical_article_id') or '')
              metas=[x for x in c['meta_items'] if isinstance(x,dict) and str(x.get('plan_slot') or '')==slot]
              rels=[x for x in c['release_items'] if isinstance(x,dict) and str(x.get('plan_slot') or '')==slot]
              if len(metas)!=1 or len(rels)!=1: raise ViewError('BOUND_RUNTIME_ITEM_NOT_UNIQUE')
              meta=dict(metas[0]); rel=dict(rels[0])
              if rel.get('canonical_article_id')!=cid: raise ViewError('BOUND_RUNTIME_CANONICAL_ID_MISMATCH')
              source_id=str(rel.get('source_snapshot_id') or '')
              if not source_id: raise ViewError('BOUND_RUNTIME_SOURCE_SNAPSHOT_ID_MISSING')
              if not c['plan_items']: raise ViewError('BOUND_PRODUCTION_PLAN_ITEMS_EMPTY')
              if not c['fact_packs']: raise ViewError('BOUND_FACT_PACKS_EMPTY')
              plans=[dict(x) for x in c['plan_items'] if isinstance(x,dict) and str(x.get('canonical_article_id') or '')==cid]
              if len(plans)!=1: raise ViewError('BOUND_PRODUCTION_PLAN_ITEM_NOT_UNIQUE')
              pi=plans[0]
              if 'plan_slot' in pi: raise ViewError('BOUND_PRODUCTION_PLAN_ITEM_SYNTHETIC_SLOT_FORBIDDEN')
              if str(pi.get('source_snapshot_id') or '')!=source_id: raise ViewError('BOUND_PRODUCTION_PLAN_SOURCE_ID_MISMATCH')
              facts=[dict(x) for x in c['fact_packs'] if isinstance(x,dict) and str(x.get('source_snapshot_id') or '')==source_id]
              if len(facts)!=1: raise ViewError('BOUND_FACT_PACK_NOT_UNIQUE')
              for k in ('title','target_keyword','category','article_type'):
                  if str(it.get(k) or '')!=str(meta.get(k) or ''): raise ViewError('CURRENT_ITEM_RUNTIME_METADATA_MISMATCH:'+k)
              for pk,mk in (('article_type','article_type'),('target_keyword','target_keyword'),('topic','title')):
                  if str(pi.get(pk) or '')!=str(meta.get(mk) or ''): raise ViewError('BOUND_PRODUCTION_PLAN_METADATA_MISMATCH:'+pk)
              cb=pi.get('category_binding'); qb=pi.get('quality_binding'); wc=qb.get('wordpress_category') if isinstance(qb,dict) else None
              category=(cb.get('slug') if isinstance(cb,dict) else None) or (wc.get('slug') if isinstance(wc,dict) else None)
              if str(category or '')!=str(meta.get('category') or ''): raise ViewError('BOUND_PRODUCTION_PLAN_CATEGORY_MISMATCH')
              return {'ctx':c,'meta':meta,'release_item':rel,'production_plan_item':pi,'fact_pack':facts[0]}
          '''
          a=replace_block(a,'def _bound_expected(it:Mapping[str,Any])->dict:','def _contract_binding',new_bound)
          old="'stage_proofs_required_value':[],'adapter_ref':HANDOFF_REL"
          new="'stage_proofs_required_value':[],'production_package_context':{'fact_pack':bound['fact_pack'],'production_plan_item':bound['production_plan_item'],'production_plan_header':bound['ctx']['plan_header'],'workflow_release_item':bound['release_item'],'workflow_release_metadata':bound['ctx']['release_metadata']},'adapter_ref':HANDOFF_REL"
          if old not in a: raise SystemExit('ACTION_HANDOFF_INSERT_POINT_MISSING')
          a=a.replace(old,new,1)
          old_tail="""    fp=q.get('fact_pack'); pi=q.get('production_plan_item'); ph=q.get('production_plan_header'); ri=q.get('workflow_release_item'); rm=q.get('workflow_release_metadata')
              if not isinstance(fp,dict) or not fp or not isinstance(pi,dict): raise ViewError('FACH_PRODUCTION_CONTEXT_INCOMPLETE')
              meta=bound['meta']; expected_pi={'canonical_article_id':it.get('canonical_article_id'),'plan_slot':it.get('plan_slot'),'article_type':meta.get('article_type'),'target_keyword':meta.get('target_keyword'),'topic':meta.get('title')}
              for k,v in expected_pi.items():
                  if pi.get(k)!=v: raise ViewError('PLAN_ITEM_IDENTITY_MISMATCH:'+k)
              if ph!=c['plan_header'] or ri!=bound['release_item'] or rm!=c['release_metadata']: raise ViewError('BOUND_RUNTIME_CONTEXT_MISMATCH')
              _validate_release_metadata_identity(rm,batch,c['count']); return q
          """.replace('              ','    ')
          new_tail="""    fp=q.get('fact_pack'); pi=q.get('production_plan_item'); ph=q.get('production_plan_header'); ri=q.get('workflow_release_item'); rm=q.get('workflow_release_metadata')
              if not isinstance(fp,dict) or not fp or not isinstance(pi,dict): raise ViewError('FACH_PRODUCTION_CONTEXT_INCOMPLETE')
              if fp!=bound['fact_pack']: raise ViewError('BOUND_FACT_PACK_CONTEXT_MISMATCH')
              if 'plan_slot' in pi: raise ViewError('PLAN_ITEM_SYNTHETIC_SLOT_FORBIDDEN')
              meta=bound['meta']; expected_pi={'canonical_article_id':it.get('canonical_article_id'),'source_snapshot_id':bound['release_item'].get('source_snapshot_id'),'article_type':meta.get('article_type'),'target_keyword':meta.get('target_keyword'),'topic':meta.get('title')}
              for k,v in expected_pi.items():
                  if pi.get(k)!=v: raise ViewError('PLAN_ITEM_IDENTITY_MISMATCH:'+k)
              cb=pi.get('category_binding'); qb=pi.get('quality_binding'); wc=qb.get('wordpress_category') if isinstance(qb,dict) else None
              category=(cb.get('slug') if isinstance(cb,dict) else None) or (wc.get('slug') if isinstance(wc,dict) else None)
              if str(category or '')!=str(meta.get('category') or ''): raise ViewError('PLAN_ITEM_CATEGORY_MISMATCH')
              if ph!=c['plan_header'] or ri!=bound['release_item'] or rm!=c['release_metadata']: raise ViewError('BOUND_RUNTIME_CONTEXT_MISMATCH')
              _validate_release_metadata_identity(rm,batch,c['count']); return q
          """.replace('              ','    ')
          if old_tail not in a: raise SystemExit('ACTION_PASS_TAIL_MISSING')
          a=a.replace(old_tail,new_tail,1)
          ACTION.write_text(a,encoding='utf-8')

          h=HANDOFF.read_text(encoding='utf-8')
          new_hruntime=r'''def _runtime_context(repo: Path, batch: str) -> dict:
              state = _load(repo / RUNTIME_STATE_REL)
              if state.get("status") != "EXECUTION_READY" or state.get("publish_allowed") is not False: raise Blocked("RUNTIME_NOT_EXECUTION_READY")
              if state.get("batch_sha256") != batch: raise Blocked("RUNTIME_BATCH_MISMATCH")
              generation=state.get('generation')
              if not isinstance(generation,int) or generation<1: raise Blocked('RUNTIME_GENERATION_INVALID')
              gbase=f'control/startmaster0107/runtime_inbox/generations/{generation:06d}'
              source_ref = str(state.get("source_snapshot_ref") or "")
              if source_ref!=gbase+'/SOURCE_SNAPSHOT.json': raise Blocked('RUNTIME_GENERATION_SOURCE_REF_MISMATCH')
              source_path = _safe_repo(repo, source_ref); source_sha = str(state.get("source_snapshot_sha256") or "")
              if not source_path.is_file() or _sha(source_path) != source_sha: raise Blocked("RUNTIME_SOURCE_SNAPSHOT_HASH_MISMATCH")
              source = _load(source_path)
              package_ref = str(state.get("production_package_ref") or "")
              if package_ref!=gbase+'/PRODUCTION_PACKAGE.json': raise Blocked('RUNTIME_GENERATION_PACKAGE_REF_MISMATCH')
              package_path = _safe_repo(repo, package_ref); package_sha = str(state.get("production_package_sha256") or "")
              if not package_path.is_file() or _sha(package_path) != package_sha: raise Blocked("RUNTIME_PRODUCTION_PACKAGE_HASH_MISMATCH")
              package = _load(package_path)
              if package.get('contract')!='PSERC_APPROVED_PRODUCTION_PACKAGE_V1': raise Blocked('RUNTIME_PRODUCTION_PACKAGE_CONTRACT_INVALID')
              release = package.get("workflow_release"); release_items = release.get("items") if isinstance(release, dict) else None
              if not isinstance(release, dict) or release.get('contract')!='WORKFLOW_SUPERVISOR_RELEASE_V2_SIGNED' or release.get('status')!='PASS' or not isinstance(release_items, list) or not release_items: raise Blocked("RUNTIME_WORKFLOW_RELEASE_INVALID")
              if release.get("exact_five_batch_sha256") != batch: raise Blocked("RUNTIME_RELEASE_BATCH_MISMATCH")
              if int(release.get("exact_five_item_count") or -1) != len(release_items): raise Blocked("RUNTIME_RELEASE_COUNT_MISMATCH")
              if release.get("wordpress_write_performed") is not False: raise Blocked("RUNTIME_RELEASE_WORDPRESS_WRITE_FORBIDDEN")
              meta_batch = source.get("next_textmachine_metadata_batch"); meta_items = meta_batch.get("items") if isinstance(meta_batch, dict) else None
              if not isinstance(meta_batch, dict) or meta_batch.get("batch_sha256") != batch or not isinstance(meta_items, list): raise Blocked("RUNTIME_METADATA_BATCH_INVALID")
              if len(meta_items) != len(release_items): raise Blocked("RUNTIME_METADATA_COUNT_MISMATCH")
              plan = package.get("production_plan"); plan_items=plan.get('items') if isinstance(plan,dict) else None
              if not isinstance(plan, dict) or plan.get("contract") not in {'production_plan_v4','production_plan_v5'} or not isinstance(plan_items,list) or not plan_items: raise Blocked("RUNTIME_PRODUCTION_PLAN_HEADER_INVALID")
              bundle=package.get('fact_pack_bundle'); fact_packs=bundle.get('fact_packs') if isinstance(bundle,dict) else None
              if not isinstance(bundle,dict) or bundle.get('contract')!='canonical_fact_pack_import_v1' or not isinstance(fact_packs,list) or not fact_packs: raise Blocked('RUNTIME_FACT_PACK_BUNDLE_INVALID')
              plan_header = dict(plan); plan_header.pop("items", None)
              release_metadata = dict(release); release_metadata.pop("items", None)
              return {"state":state,"release_items":release_items,"meta_items":meta_items,"plan_items":plan_items,"fact_packs":fact_packs,"plan_header":plan_header,"release_metadata":release_metadata,"source_sha256":source_sha,"package_sha256":package_sha}
          '''
          h=replace_block(h,'def _runtime_context(repo: Path, batch: str) -> dict:','def _bound_item',new_hruntime)
          new_hbound=r'''def _bound_item(ctx: Mapping[str, Any], canonical_id: str, slot: str) -> tuple[dict, dict, dict, dict]:
              metas=[x for x in ctx["meta_items"] if isinstance(x,dict) and str(x.get("plan_slot") or "")==slot]
              rels=[x for x in ctx["release_items"] if isinstance(x,dict) and str(x.get("plan_slot") or "")==slot]
              if len(metas)!=1 or len(rels)!=1: raise Blocked("BOUND_RUNTIME_ITEM_NOT_UNIQUE")
              meta=dict(metas[0]); rel=dict(rels[0])
              if rel.get("canonical_article_id")!=canonical_id: raise Blocked("BOUND_RUNTIME_CANONICAL_ID_MISMATCH")
              source_id=str(rel.get('source_snapshot_id') or '')
              if not source_id: raise Blocked('BOUND_RUNTIME_SOURCE_SNAPSHOT_ID_MISSING')
              plans=[dict(x) for x in ctx['plan_items'] if isinstance(x,dict) and str(x.get('canonical_article_id') or '')==canonical_id]
              if len(plans)!=1: raise Blocked('BOUND_PRODUCTION_PLAN_ITEM_NOT_UNIQUE')
              item=plans[0]
              if 'plan_slot' in item: raise Blocked('BOUND_PRODUCTION_PLAN_ITEM_SYNTHETIC_SLOT_FORBIDDEN')
              if str(item.get('source_snapshot_id') or '')!=source_id: raise Blocked('BOUND_PRODUCTION_PLAN_SOURCE_ID_MISMATCH')
              facts=[dict(x) for x in ctx['fact_packs'] if isinstance(x,dict) and str(x.get('source_snapshot_id') or '')==source_id]
              if len(facts)!=1: raise Blocked('BOUND_FACT_PACK_NOT_UNIQUE')
              return meta,rel,item,facts[0]
          '''
          h=replace_block(h,'def _bound_item(ctx: Mapping[str, Any], canonical_id: str, slot: str)','def _validate_raw_context',new_hbound)
          new_hvalidate=r'''def _validate_raw_context(request: Mapping[str, Any], ctx: Mapping[str, Any], meta: Mapping[str, Any], release_item: Mapping[str, Any], expected_item: Mapping[str, Any], expected_fact: Mapping[str, Any]) -> dict:
              fact_pack=request.get("fact_pack"); item=request.get("production_plan_item")
              if not isinstance(fact_pack,dict) or not fact_pack: raise Blocked("BOUND_FACT_PACK_MISSING")
              if not isinstance(item,dict) or not item: raise Blocked("BOUND_PRODUCTION_PLAN_ITEM_MISSING")
              if fact_pack!=expected_fact: raise Blocked('BOUND_FACT_PACK_MISMATCH')
              if item!=expected_item: raise Blocked('BOUND_PRODUCTION_PLAN_ITEM_OBJECT_MISMATCH')
              if request.get("production_plan_header")!=ctx["plan_header"]: raise Blocked("BOUND_PRODUCTION_PLAN_HEADER_MISMATCH")
              if request.get("workflow_release_item")!=release_item: raise Blocked("BOUND_WORKFLOW_RELEASE_ITEM_MISMATCH")
              if request.get("workflow_release_metadata")!=ctx["release_metadata"]: raise Blocked("BOUND_WORKFLOW_RELEASE_METADATA_MISMATCH")
              if str(release_item.get('plan_slot') or '')!=str(request.get('plan_slot') or ''): raise Blocked('BOUND_RELEASE_PLAN_SLOT_MISMATCH')
              if release_item.get('canonical_article_id')!=request.get('canonical_article_id'): raise Blocked('BOUND_RELEASE_CANONICAL_ID_MISMATCH')
              if 'plan_slot' in item: raise Blocked('BOUND_PRODUCTION_PLAN_ITEM_SYNTHETIC_SLOT_FORBIDDEN')
              expected={"canonical_article_id":request["canonical_article_id"],"source_snapshot_id":release_item.get('source_snapshot_id'),"article_type":meta.get("article_type"),"target_keyword":meta.get("target_keyword"),"topic":meta.get("title")}
              for k,v in expected.items():
                  if item.get(k)!=v: raise Blocked("BOUND_PRODUCTION_PLAN_ITEM_MISMATCH:"+k)
              cb=item.get('category_binding'); quality=item.get("quality_binding"); category=quality.get("wordpress_category") if isinstance(quality,dict) else None
              slug=(cb.get('slug') if isinstance(cb,dict) else None) or (category.get('slug') if isinstance(category,dict) else None)
              if str(slug or '')!=str(meta.get('category') or ''): raise Blocked("BOUND_WORDPRESS_CATEGORY_MISMATCH")
              if isinstance(category,dict) and category.get('taxonomy') not in (None,'category'): raise Blocked('BOUND_WORDPRESS_CATEGORY_TAXONOMY_MISMATCH')
              return dict(item)
          '''
          h=replace_block(h,'def _validate_raw_context(request: Mapping[str, Any]','def _validate_worker_stage_proofs',new_hvalidate)
          old='ctx=_runtime_context(repo,batch); meta,release_item=_bound_item(ctx,canonical_id,slot); item=_validate_raw_context(request,ctx,meta,release_item)'
          new='ctx=_runtime_context(repo,batch); meta,release_item,expected_item,expected_fact=_bound_item(ctx,canonical_id,slot); item=_validate_raw_context(request,ctx,meta,release_item,expected_item,expected_fact)'
          if old not in h: raise SystemExit('HANDOFF_MATERIALIZE_BIND_POINT_MISSING')
          h=h.replace(old,new,1)
          oldphp="if((string)($plan['contract']??'')!=='production_plan_v4'){fwrite(STDERR,\"PRODUCTION_PLAN_CONTRACT_INVALID\\n\");exit(2);}"
          newphp="if(!in_array((string)($plan['contract']??''),['production_plan_v4','production_plan_v5'],true)){fwrite(STDERR,\"PRODUCTION_PLAN_CONTRACT_INVALID\\n\");exit(2);}"
          if oldphp not in h: raise SystemExit('HANDOFF_PHP_PLAN_CONTRACT_POINT_MISSING')
          h=h.replace(oldphp,newphp,1)
          HANDOFF.write_text(h,encoding='utf-8')

          step=json.loads(STEP.read_text(encoding='utf-8'))
          ins=step['instruction']
          old='- Worker erzeugt ausschließlich reale Roharbeitsprodukte: Recherche/fact_pack, finaler Artikel, production_plan-Kontext und workflow_release-Kontext.'
          new='- Worker erzeugt ausschließlich die zulässigen realen Roharbeitsprodukte; Recherche/fact_pack, production_plan-Kontext und workflow_release-Kontext werden NICHT vom Worker erzeugt, sondern unverändert aus fachworkflow_handoff.production_package_context übernommen. Der Worker erzeugt den finalen Artikel gemäß gebundenem Fachworkflow.'
          if old not in ins: raise SystemExit('STEP_WORKER_SOURCE_TEXT_MISSING')
          ins=ins.replace(old,new,1)
          old='fact_pack=der real erzeugte fact_pack; production_plan_item=der reale aktuelle production_plan-Item-Kontext; production_plan_header=der reale production_plan-Header ohne items; workflow_release_item=der reale aktuelle workflow_release-Item-Kontext; workflow_release_metadata=die real gebundene workflow_release-Metadatenstruktur.'
          new='fact_pack=fachworkflow_handoff.production_package_context.fact_pack; production_plan_item=fachworkflow_handoff.production_package_context.production_plan_item; production_plan_header=fachworkflow_handoff.production_package_context.production_plan_header; workflow_release_item=fachworkflow_handoff.production_package_context.workflow_release_item; workflow_release_metadata=fachworkflow_handoff.production_package_context.workflow_release_metadata.'
          if old not in ins: raise SystemExit('STEP_REQUEST_SOURCE_TEXT_MISSING')
          ins=ins.replace(old,new,1)
          step['instruction']=ins
          for row in step['authorized_inputs']:
              if row.get('ref')==str(ACTION): row['sha256']=sh(ACTION)
              if row.get('ref')==str(HANDOFF): row['sha256']=sh(HANDOFF)
          dump(STEP,step)

          state=json.loads(STATE.read_text(encoding='utf-8')); step_sha=sh(STEP)
          state['execution_gate']['bundle_sha256']=step_sha
          if isinstance(state.get('execution_gate_rearm_target'),dict): state['execution_gate_rearm_target']['bundle_sha256']=step_sha
          dump(STATE,state)
          start=json.loads(START.read_text(encoding='utf-8')); start['current_state_sha256']=sh(STATE); dump(START,start)

          test_source=r'''from __future__ import annotations
          import copy, hashlib, importlib.util, json, unittest
          from pathlib import Path
          from contextlib import contextmanager

          HERE=Path(__file__).resolve().parent; REPO=HERE.parents[1]
          STATE=REPO/'control/startmaster0107/runtime_inbox/RUNTIME_INBOX_STATE.json'
          PACKAGE=REPO/'control/startmaster0107/runtime_inbox/generations/000001/PRODUCTION_PACKAGE.json'
          SOURCE=REPO/'control/startmaster0107/runtime_inbox/generations/000001/SOURCE_SNAPSHOT.json'
          CONTRACT=REPO/'control/startmaster0107/runtime_inbox/RUNTIME_BATCH_SLOT_CONTRACT_V1.json'
          def mod(path,name):
              s=importlib.util.spec_from_file_location(name,path); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); return m
          guard=mod(REPO/'control/startmaster0107/runtime_inbox/runtime_batch_slot_guard.py','pr221_guard')
          action=mod(REPO/'control/single-door-boundary/codex_current_action.py','pr221_action')
          handoff=mod(REPO/'control/startmaster0107/fachworkflow_proof_handoff.py','pr221_handoff')
          def dump(p,o): p.write_text(json.dumps(o,ensure_ascii=False,indent=2)+'\n',encoding='utf-8')
          def sha(p): return hashlib.sha256(p.read_bytes()).hexdigest()

          class DirectPackageBindingTest(unittest.TestCase):
              def setUp(self): self.state_bytes=STATE.read_bytes(); self.package_bytes=PACKAGE.read_bytes()
              def tearDown(self): STATE.write_bytes(self.state_bytes); PACKAGE.write_bytes(self.package_bytes)
              def package(self,version='production_plan_v5'):
                  state=json.loads(self.state_bytes); source=json.loads(SOURCE.read_text(encoding='utf-8')); old=json.loads(self.package_bytes)
                  metas=source['next_textmachine_metadata_batch']['items']; oldrels=old['workflow_release']['items']
                  plans=[]; facts=[]; rels=[]
                  for i,(meta,oldrel) in enumerate(zip(metas,oldrels)):
                      sid=f'fresh-source-{i+1}'; cid=oldrel['canonical_article_id']
                      pi={'canonical_article_id':cid,'source_snapshot_id':sid,'article_type':meta['article_type'],'target_keyword':meta['target_keyword'],'topic':meta['title'],'canonical_article':{'body_html':'test body'}}
                      if version=='production_plan_v5': pi['category_binding']={'slug':meta['category']}
                      else: pi['quality_binding']={'wordpress_category':{'slug':meta['category'],'taxonomy':'category'}}
                      plans.append(pi); facts.append({'contract':'canonical_fact_pack_v1','source_snapshot_id':sid,'fact_pack_id':f'fp-{i+1}'})
                      rels.append({'plan_slot':meta['plan_slot'],'canonical_article_id':cid,'source_snapshot_id':sid})
                  bundle={'contract':'canonical_fact_pack_import_v1','fact_packs':facts}
                  plan={'contract':version,'items':plans}
                  bsha=guard.stable_hash(bundle); psha=guard.stable_hash(plan)
                  release={'contract':'WORKFLOW_SUPERVISOR_RELEASE_V2_SIGNED','status':'PASS','exact_five_batch_sha256':state['batch_sha256'],'exact_five_item_count':len(rels),'items':rels,'production_plan_sha256':psha,'fact_pack_bundle_sha256':bsha,'content_generation_performed_by_supervisor':False,'wordpress_write_performed':False,'signature_algorithm':'ED25519','signature_b64':'test-signature'}
                  rsha=guard.stable_hash(release)
                  env={'contract':'PSERC_APPROVED_PRODUCTION_PACKAGE_V1','fact_pack_bundle_sha256':bsha,'production_plan_sha256':psha,'workflow_release_sha256':rsha,'package_id':guard.stable_hash({'contract':'PSERC_APPROVED_PRODUCTION_PACKAGE_V1','fact_pack_bundle_sha256':bsha,'production_plan_sha256':psha,'workflow_release_sha256':rsha}),'source':'FRESH_TEST_ONLY','fact_pack_bundle':bundle,'production_plan':plan,'workflow_release':release}
                  env['package_payload_sha256']=guard.stable_hash(env)
                  return env
              def install(self,env):
                  dump(PACKAGE,env); st=json.loads(self.state_bytes); st['production_package_sha256']=sha(PACKAGE); dump(STATE,st)
              def current_item(self):
                  c=action._runtime_context(); meta=dict(c['meta_items'][0]); rel=next(x for x in c['release_items'] if x['plan_slot']==meta['plan_slot'])
                  return {'canonical_article_id':rel['canonical_article_id'],'plan_slot':meta['plan_slot'],'title':meta['title'],'target_keyword':meta['target_keyword'],'category':meta['category'],'article_type':meta['article_type']}
              def test_v4_v5_positive_and_slotless(self):
                  for version in ('production_plan_v4','production_plan_v5'):
                      self.install(self.package(version)); out=guard.validate(REPO,CONTRACT,STATE); self.assertEqual('RUNTIME_INPUTS_BOUND',out['status'])
                      item=self.current_item(); bound=action._bound_expected(item); self.assertNotIn('plan_slot',bound['production_plan_item'])
                      view=action.augment_current_action(REPO,{'allowed_output_root':'.pferde-quarantine/test/','item_receipt_schema':{}},item)
                      pc=view['fachworkflow_handoff']['production_package_context']; self.assertEqual(bound['fact_pack'],pc['fact_pack']); self.assertEqual(bound['production_plan_item'],pc['production_plan_item'])
                      ctx=handoff._runtime_context(REPO,action._runtime_context()['batch']); meta,rel,pi,fp=handoff._bound_item(ctx,item['canonical_article_id'],item['plan_slot'])
                      req={'canonical_article_id':item['canonical_article_id'],'plan_slot':item['plan_slot'],'fact_pack':fp,'production_plan_item':pi,'production_plan_header':ctx['plan_header'],'workflow_release_item':rel,'workflow_release_metadata':ctx['release_metadata']}
                      self.assertEqual(pi,handoff._validate_raw_context(req,ctx,meta,rel,pi,fp))
              def test_package_and_identity_negatives(self):
                  env=self.package(); self.install(env); item=self.current_item()
                  st=json.loads(STATE.read_text()); st['production_package_sha256']='0'*64; dump(STATE,st)
                  with self.assertRaises(action.ViewError): action._runtime_context()
                  self.install(env); st=json.loads(STATE.read_text()); st['generation']=2; dump(STATE,st)
                  with self.assertRaises(action.ViewError): action._runtime_context()
                  for mut in ('empty_plan','empty_facts','dup_plan','dup_fact','synthetic_slot','source_mismatch'):
                      x=copy.deepcopy(env)
                      if mut=='empty_plan': x['production_plan']['items']=[]
                      elif mut=='empty_facts': x['fact_pack_bundle']['fact_packs']=[]
                      elif mut=='dup_plan': x['production_plan']['items'].append(copy.deepcopy(x['production_plan']['items'][0]))
                      elif mut=='dup_fact': x['fact_pack_bundle']['fact_packs'].append(copy.deepcopy(x['fact_pack_bundle']['fact_packs'][0]))
                      elif mut=='synthetic_slot': x['production_plan']['items'][0]['plan_slot']=x['workflow_release']['items'][0]['plan_slot']
                      elif mut=='source_mismatch': x['production_plan']['items'][0]['source_snapshot_id']='wrong-source'
                      self.install(x)
                      with self.assertRaises(action.ViewError): action._bound_expected(item)
                  self.install(env); c=action._runtime_context(); meta=dict(c['meta_items'][0]); rel=dict(c['release_items'][0]); rel['canonical_article_id']='article:wrong'
                  c['release_items'][0]=rel
                  with self.assertRaises(handoff.Blocked): handoff._bound_item(c,item['canonical_article_id'],item['plan_slot'])
              def test_all_five_request_context_tamper_and_stage_proofs(self):
                  self.install(self.package()); item=self.current_item(); ctx=handoff._runtime_context(REPO,action._runtime_context()['batch']); meta,rel,pi,fp=handoff._bound_item(ctx,item['canonical_article_id'],item['plan_slot'])
                  base={'canonical_article_id':item['canonical_article_id'],'plan_slot':item['plan_slot'],'fact_pack':fp,'production_plan_item':pi,'production_plan_header':ctx['plan_header'],'workflow_release_item':rel,'workflow_release_metadata':ctx['release_metadata']}
                  for field in ('fact_pack','production_plan_item','production_plan_header','workflow_release_item','workflow_release_metadata'):
                      bad=copy.deepcopy(base); bad[field]=copy.deepcopy(bad[field]); bad[field]['tampered']=True
                      with self.assertRaises(handoff.Blocked): handoff._validate_raw_context(bad,ctx,meta,rel,pi,fp)
                  handoff._validate_worker_stage_proofs([])
                  for bad in ([{'stage':'x'}],None,{}):
                      with self.assertRaises(handoff.Blocked): handoff._validate_worker_stage_proofs(bad)
          if __name__=='__main__': unittest.main()
          '''
          TEST.write_text(test_source.replace('          ','',1) if False else '\n'.join(line[10:] if line.startswith('          ') else line for line in test_source.splitlines())+'\n',encoding='utf-8')
          print('PR221_PATCH_APPLIED')
          PY
      - name: Compile and direct positive-negative tests
        shell: bash
        run: |
          set -euo pipefail
          python3 -m py_compile control/single-door-boundary/codex_current_action.py control/startmaster0107/fachworkflow_proof_handoff.py control/startmaster0107/test_fachworkflow_proof_handoff.py
          python3 -m unittest -v control/startmaster0107/test_fachworkflow_proof_handoff.py
          python3 control/single-door-boundary/codex_current_action.py selftest
          python3 control/startmaster0107/fachworkflow_proof_handoff.py selftest
          ! grep -R "source_owner_contexts\|PSERC_REALTEST_7_READY\|recovery_sources" control/single-door-boundary/codex_current_action.py control/startmaster0107/fachworkflow_proof_handoff.py control/startmaster0107/STEP_107007_RUN_NEW_ARTICLE_BATCH_NO_STOP.json
      - name: Full M01-M36 on same candidate with fresh package only where R_001 requires it
        shell: bash
        run: |
          set -euo pipefail
          cat > /tmp/run_m01_m36.py <<'PY'
          import copy, hashlib, importlib.util, json
          from contextlib import contextmanager
          from pathlib import Path
          REPO=Path('.').resolve()
          def mod(path,name):
              s=importlib.util.spec_from_file_location(name,path); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); return m
          m=mod(REPO/'control/startmaster0107/HOBBYRAUM_M01_M33_REGRESSION.py','reg')
          guard=mod(REPO/'control/startmaster0107/runtime_inbox/runtime_batch_slot_guard.py','guard_for_reg')
          statep=REPO/'control/startmaster0107/runtime_inbox/RUNTIME_INBOX_STATE.json'; pkgp=REPO/'control/startmaster0107/runtime_inbox/generations/000001/PRODUCTION_PACKAGE.json'; srcp=REPO/'control/startmaster0107/runtime_inbox/generations/000001/SOURCE_SNAPSHOT.json'
          orig_state=statep.read_bytes(); orig_pkg=pkgp.read_bytes()
          def dump(p,o): p.write_text(json.dumps(o,ensure_ascii=False,indent=2)+'\n',encoding='utf-8')
          @contextmanager
          def fresh():
              try:
                  st=json.loads(orig_state); src=json.loads(srcp.read_text()); old=json.loads(orig_pkg); metas=src['next_textmachine_metadata_batch']['items']; oldrels=old['workflow_release']['items']; plans=[]; facts=[]; rels=[]
                  for i,(meta,oldrel) in enumerate(zip(metas,oldrels)):
                      sid=f'reg-fresh-{i+1}'; cid=oldrel['canonical_article_id']; plans.append({'canonical_article_id':cid,'source_snapshot_id':sid,'article_type':meta['article_type'],'target_keyword':meta['target_keyword'],'topic':meta['title'],'category_binding':{'slug':meta['category']}}); facts.append({'contract':'canonical_fact_pack_v1','source_snapshot_id':sid}); rels.append({'plan_slot':meta['plan_slot'],'canonical_article_id':cid,'source_snapshot_id':sid})
                  bundle={'contract':'canonical_fact_pack_import_v1','fact_packs':facts}; plan={'contract':'production_plan_v5','items':plans}; bsha=guard.stable_hash(bundle); psha=guard.stable_hash(plan); release={'contract':'WORKFLOW_SUPERVISOR_RELEASE_V2_SIGNED','status':'PASS','exact_five_batch_sha256':st['batch_sha256'],'exact_five_item_count':len(rels),'items':rels,'production_plan_sha256':psha,'fact_pack_bundle_sha256':bsha,'wordpress_write_performed':False,'signature_algorithm':'ED25519','signature_b64':'reg-test'}; rsha=guard.stable_hash(release); env={'contract':'PSERC_APPROVED_PRODUCTION_PACKAGE_V1','fact_pack_bundle_sha256':bsha,'production_plan_sha256':psha,'workflow_release_sha256':rsha,'package_id':guard.stable_hash({'contract':'PSERC_APPROVED_PRODUCTION_PACKAGE_V1','fact_pack_bundle_sha256':bsha,'production_plan_sha256':psha,'workflow_release_sha256':rsha}),'source':'REGRESSION_TEST_ONLY','fact_pack_bundle':bundle,'production_plan':plan,'workflow_release':release}; env['package_payload_sha256']=guard.stable_hash(env); dump(pkgp,env); st['production_package_sha256']=hashlib.sha256(pkgp.read_bytes()).hexdigest(); dump(statep,st); yield
              finally: statep.write_bytes(orig_state); pkgp.write_bytes(orig_pkg)
          def wrap(fn):
              def inner():
                  with fresh(): return fn()
              return inner
          m.m26=wrap(m.m26); m.m28=wrap(m.m28); m.m31=wrap(m.m31)
          m.CASES=[(mid, {'M26':m.m26,'M28':m.m28,'M31':m.m31}.get(mid,fn)) for mid,fn in m.CASES]
          raise SystemExit(m.main([]))
          PY
          python3 /tmp/run_m01_m36.py
      - name: Final integrity, remove all temporary scaffolding, commit
        shell: bash
        env:
          BASE_SHA: 2f0a3a25da77e95fb28e7da4f500aa3c50ac9910
        run: |
          set -euo pipefail
          git diff --check
          git diff --exit-code -- control/startmaster0107/runtime_inbox/runtime_batch_slot_lifecycle.py control/startmaster0107/runtime_inbox/runtime_batch_slot_guard.py control/startmaster0107/runtime_inbox/RUNTIME_BATCH_SLOT_CONTRACT_V1.json
          git show "$BASE_SHA:AGENTS.md" > AGENTS.md
          git rm .github/workflows/pr221-kiss-finalize.yml
          git diff --check
          test -z "$(git diff --name-only "$BASE_SHA" -- .github/workflows AGENTS.md control/startmaster0107/runtime_inbox/runtime_batch_slot_lifecycle.py control/startmaster0107/runtime_inbox/runtime_batch_slot_guard.py control/startmaster0107/runtime_inbox/RUNTIME_BATCH_SLOT_CONTRACT_V1.json)"
          python3 - <<'PY'
          import hashlib,json
          from pathlib import Path
          step=Path('control/startmaster0107/STEP_107007_RUN_NEW_ARTICLE_BATCH_NO_STOP.json'); state=Path('control/startmaster0107/CURRENT_STATE.json'); root=Path('control/startmaster0107/PFERDE_ATELIER_START_HERE.json')
          S=lambda p:hashlib.sha256(p.read_bytes()).hexdigest()
          st=json.loads(state.read_text()); r=json.loads(root.read_text()); assert st['execution_gate']['bundle_sha256']==S(step); assert st['execution_gate_rearm_target']['bundle_sha256']==S(step); assert r['current_state_sha256']==S(state); assert st['current_execution_blocker']['resolved'] is False; assert st['publish_allowed'] is False
          print('STEP_CURRENT_STATE_START_HERE_HASH_CHAIN_PASS')
          PY
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add control/single-door-boundary/codex_current_action.py control/startmaster0107/fachworkflow_proof_handoff.py control/startmaster0107/STEP_107007_RUN_NEW_ARTICLE_BATCH_NO_STOP.json control/startmaster0107/CURRENT_STATE.json control/startmaster0107/PFERDE_ATELIER_START_HERE.json control/startmaster0107/test_fachworkflow_proof_handoff.py AGENTS.md
          git commit -m "Bind R_001 to existing approved production package"
          git push origin HEAD:hobbyroom/107007-r001-kiss-core-20260910-v2
