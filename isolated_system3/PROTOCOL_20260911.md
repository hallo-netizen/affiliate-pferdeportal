# SYSTEM 3 — PROTOKOLL 2026-09-11

Autoritative Statusquelle: `isolated_system3/CURRENT_STATE.json`.

## Dauerhafte Befunde

- System 3 bleibt isoliert. Kein Runtime-/Workflow-Code, Handoff, Worker, Gate, State Machine oder Signer aus System 1/2 wird übernommen.
- Autoritative Regelquellen sind PPM 6.7.9, PSERC und vollständiges PSTE. Sie wurden hashgebunden, versiegelt und gegen Manipulation negativ geprüft.
- Canonical Artikeltypen wurden aus den versiegelten Regeln ermittelt; das aktuelle Testthema ist `FAQ`.
- FAQ-Pflichtpfad: `item.quality_binding.type_meta.primary_question`.
- Canonical Fact-Pack: `canonical_fact_pack_import_v1`, mindestens drei `FULLY_SUPPORTED` Claims mit realen Quellen und Evidence-SHA256. Die Artikelerzeugung darf fehlende Fakten nicht erfinden oder nachrecherchieren.
- Negativtest ohne Fact Pack: BLOCKED wie vorgesehen.
- Negativtest mit Fact Pack aber ohne interne Linkziele: BLOCKED wie vorgesehen.
- Erster vollständiger FAQ-Test mit damals extern eingesetzten Testlinks: `SYSTEM3_RETURN_PASS`; unabhängiger Artifact-Audit ebenfalls PASS, SHA256 `40075c440c30b393c085f93f4b6f5a1b141877ed33995bae7f945d2f73b0d69d`.
- Dieser frühere PASS bleibt Nachweis für Fact-/Artikel-/Regelausführung, ist aber **nicht mehr der endgültige Linkregel-Nachweis**, weil später die bereits vorhandenen festen Regendecken-Linkbindungen wiedergefunden wurden.

## Feste interne Linkregeln — hart bestätigt

Die kanonischen Regeln verlangen exakt drei vor der Texterstellung gebundene interne Links in `runtime_order.links`:

- `parent_category`
- `semantic_related`
- `further_information`

Pflicht je Link: `role`, `href`, `anchor`, `reason`, `section_id`.

Weitere feste Constraints:

- nur relative interne URLs;
- externe URLs verboten;
- Ziele nur veröffentlichte WordPress-Objekte `page` oder `category`;
- `post` als Ziel verboten;
- drei Links in drei verschiedenen Inhaltsblöcken;
- `further_information.section_id` exakt `further_information`;
- nicht gebundene interne Links blockieren;
- automatischer Ersatz verboten;
- Codex/Artikelgenerator darf fehlende Links nicht recherchieren, erfinden, ersetzen, reparieren oder neu zuordnen;
- vor einem WordPress-Write ist read-only Live-Revalidierung einschließlich URL-zu-Objekt-ID-Auflösung erforderlich.

## Wiedergefundene feste Regendecken-Bindung

In der bereits vorhandenen Datei `02_PRODUKTIONSPLAN_3_ENTWUERFE.json` wurden `link_bindings` und `portal_link_registry` mit den fertigen Regendecken-Bindungen gefunden. Damit war die frühere Aussage, es fehle eine Link-Vorauswahl-Autorität für dieses Fixture, zu breit.

Für `FAQ Regendecken` sind fest gebunden:

1. `parent_category`
   - href: `/ausruestung/ausruestung-decken/pferdedecken-regendecken/`
   - anchor: `Regendecken`
   - reason: `Die übergeordnete Fachseite ordnet Regendecken und ihren vorgesehenen Einsatz ein.`
   - section_id: `answer`
2. `semantic_related`
   - href: `/ausruestung/ausruestung-decken/`
   - anchor: `Pferdedecken`
   - reason: `Die übergeordnete Deckenseite ergänzt Material-, Einsatz- und Auswahlgrundlagen.`
   - section_id: `details`
3. `further_information`
   - href: `/journal/`
   - anchor: `Journal`
   - reason: `Das Journal enthält ergänzende Ratgeber und vertiefende Praxisinformationen.`
   - section_id: `further_information`

Diese drei Bindungen wurden unverändert in `isolated_system3/live_fixture/wordpress_input.json` übernommen. Fixture-Commit: `9f3ba4d848d0cd513d4a9727ceedfdbe0d687cb0`.

## 7/7-Bestand

- `GEN1_7_ARTIKEL_PSERC_APPROVED_PRODUCTION_PACKAGE_107008_FINAL.json`
- SHA256 `8536206bbe34d3cad197186f26870cb75f4f6bb5a7737d4be3684b6522e5feb8`
- Status `ENDSTEMPEL_PASS`
- exakt 7 Artikel
- `publish_allowed=false`
- zugehöriger WXR-Bestand: `GEN1_7_ARTIKEL_KURZWEG_WORDPRESS_IMPORT_AKTUELL.xml`
- erneuter Upload ist nicht erforderlich.

## Tests nach Linkkorrektur

- Fixture mit festen Regendecken-Links: Commit `9f3ba4d848d0cd513d4a9727ceedfdbe0d687cb0`.
- Deterministic Entrance Gate auf diesem Commit: SUCCESS.
- Danach wurde nur `CURRENT_STATE.json` geändert; Compare `9f3ba4d8..ff2300b6` zeigt keine Fixture-/Runtime-Änderung.
- Deterministic Entrance Gate auf `ff2300b6add3e0183e12209cc02d6132e4c3c021`: SUCCESS.
- Neuer kanonischer Retest wurde auf dem unveränderten Fixed-Link-Fixture angefordert.
- **Der Retest wurde nicht ausgeführt**, weil Codex vor Ausführung meldete: `You have reached your Codex usage limits.`
- Diese Meldung ist **kein Artikel-FAIL, kein Linkregel-FAIL und kein Regel-FAIL**.

## Aktueller belastbarer Stand

- Feste Linkregeln: **GEKLÄRT / NICHT MEHR OFFEN**.
- Feste Regendecken-FAQ-Linkbindungen: **GEFUNDEN UND IM FIXTURE GEBUNDEN**.
- CI auf korrigiertem Fixture: **PASS**.
- Kanonischer Artikel-Retest mit den festen Links: **NICHT AUSGEFÜHRT wegen Codex-Usage-Limit**.
- Produktion/Publish/Merge: weiterhin nicht erlaubt.
- Kein WordPress-Write, kein Publish, kein Merge.

## Aktueller STOP

`CODEX_USAGE_LIMIT_REACHED`

Der einzige aktuelle STOP ist die fehlende Codex-Ausführungskapazität für den bereits vorbereiteten Fixed-Link-Retest. Vor diesem Retest darf kein neuer Reparaturweg geöffnet und keine Linkregel erneut verändert werden.

## Verbindliche NEXT ACTION

Sobald Codex-Ausführung wieder verfügbar ist:

1. exakt denselben kanonischen Artikeltest auf dem unveränderten Fixed-Link-Fixture erneut starten;
2. nur `SYSTEM3_RETURN_PASS` oder den ersten echten `SYSTEM3_RETURN_BLOCKED` akzeptieren;
3. bei PASS Artifact erneut unabhängig gegen die versiegelten Regeln auditieren;
4. danach denselben bestehenden System-3-Weg ohne neuen Runner auf den vorhandenen 7/7-Bestand erweitern.

PPM, PSERC, PSTE, Fach-/SEO-/Qualitätsregeln und Linkregeln bleiben unangetastet.

## Parallelwege

- PR227: Architektur-/Regelbindungs-Prototyp, Draft, nicht mergen.
- PR232: Codex-Liveweg und aktueller System-3-Test, Draft, nicht mergen.
- Andere Textsysteme sind keine System-3-Statusquelle.
