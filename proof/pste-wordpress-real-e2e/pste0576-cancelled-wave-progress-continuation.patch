--- a/includes/class-pste-breadth-research-queue.php
+++ b/includes/class-pste-breadth-research-queue.php
@@ -43,12 +43,17 @@
         $candidates=self::openFamilies($baseline);
         if(!$candidates&&$mode!=='TARGET_USABLE_CANDIDATES')throw new RuntimeException('PSTE_BREADTH_NO_OPEN_FAMILIES');
         $selected=$candidates?self::spread($candidates,max(1,min($maxItems,$initialItems))):[];
-        $baselineUsable=[];$contextBinding=[];
+        $baselineUsable=[];$contextBinding=[];$continuation=null;
         if($mode==='TARGET_USABLE_CANDIDATES'){
             $baselineUsable=PSTE_Repository::usablePlanningTopicKeys();
             $context=PSTE_Context_Refresh::status();if((string)($context['status']??'')!=='COMPLETE')throw new RuntimeException('PSTE_PRODUCTION_WAVE_CONTEXT_COMPLETE_REQUIRED');
             $contextBinding=PSTE_Context_Index_Store::runtimeBinding($context,$baseline);
+            $continuation=self::cancelledWaveContinuation($existing,$targetUsable,$maxItems,$contextBinding);
+            if(is_array($continuation))$baselineUsable=(array)$continuation['baseline_usable_topic_keys'];
         }
         $backlogReuse=$mode==='TARGET_USABLE_CANDIDATES'?self::canReuseBacklogAudit($contextBinding):false;
         $backlogCheckpoint=(!$backlogReuse&&$mode==='TARGET_USABLE_CANDIDATES')?self::reusableBacklogCheckpoint($existing,$contextBinding):null;
@@ -58,10 +63,17 @@
             'contract'=>self::CONTRACT,'version'=>self::VERSION,'queue_uuid'=>$uuid,'status'=>'RUNNING',
             'mode'=>$mode,'requested_count'=>$mode==='FIXED_FAMILY_COUNT'?count($items):$targetUsable,
             'target_usable'=>$targetUsable,'max_items'=>$maxItems,'usable_candidate_count'=>0,'raw_usable_candidate_count'=>0,'usable_topic_keys'=>[],'baseline_usable_topic_keys'=>$baselineUsable,'completion_reason'=>'',
+            'continued_from_queue_uuid'=>is_array($continuation)?(string)$continuation['queue_uuid']:'',
+            'continued_usable_candidate_count'=>0,
             'local_backlog_complete'=>$mode!=='TARGET_USABLE_CANDIDATES'||$backlogReuse,'local_backlog_reused'=>$backlogReuse,'local_backlog_resumed'=>is_array($backlogCheckpoint),'local_backlog_cursor'=>is_array($backlogCheckpoint)?(int)($backlogCheckpoint['cursor']??0):0,'local_backlog_processed'=>is_array($backlogCheckpoint)?(int)($backlogCheckpoint['processed_total']??0):0,'local_backlog_promoted'=>0,'local_backlog_provider_calls'=>0,'local_backlog_checkpoint'=>is_array($backlogCheckpoint)?$backlogCheckpoint:[],'context_binding'=>$contextBinding,
             'item_count'=>count($items),'current_index'=>0,'completed_count'=>0,'blocked_count'=>0,'items'=>$items,
             'last_error'=>'','coordination_wait_code'=>'','created_at_utc'=>$now,'updated_at_utc'=>$now,
         ];
+        if(is_array($continuation)){
+            $counted=self::countNewUniqueUsableTopics($queue);
+            $queue['usable_topic_keys']=$counted['keys'];$queue['usable_candidate_count']=count($counted['keys']);
+            $queue['continued_usable_candidate_count']=$queue['usable_candidate_count'];
+        }
         self::save($queue);
         return self::summary($queue);
         }finally{self::releaseLaunchLock($launchToken);}
@@ -143,6 +155,29 @@
     }
 
 
+    /** Continue the same target wave after a deliberate safe stop; never redefine its zero line. */
+    private static function cancelledWaveContinuation($existing,int $targetUsable,int $maxItems,array $binding): ?array {
+        if(!is_array($existing))return null;
+        if((string)($existing['status']??'')!=='CANCELLED'||(string)($existing['completion_reason']??'')!=='CANCELLED_BY_USER')return null;
+        if((string)($existing['mode']??'')!=='TARGET_USABLE_CANDIDATES')return null;
+        if((int)($existing['target_usable']??0)!==$targetUsable||(int)($existing['max_items']??0)!==$maxItems)return null;
+        if(!array_key_exists('baseline_usable_topic_keys',$existing))return null;
+        $oldBinding=(array)($existing['context_binding']??[]);
+        if(($oldBinding['contract']??'')!=='PSTE_CONTEXT_INDEX_RUNTIME_BINDING_V2'||($binding['contract']??'')!=='PSTE_CONTEXT_INDEX_RUNTIME_BINDING_V2')return null;
+        foreach(['baseline_sha256','inventory_sha256','structure_sha256','editorial_plan_sha256'] as $key){
+            $old=(string)($oldBinding[$key]??'');$current=(string)($binding[$key]??'');
+            if($old===''||$current===''||!hash_equals($old,$current))return null;
+        }
+        $queueUuid=trim((string)($existing['queue_uuid']??''));if($queueUuid==='')return null;
+        $set=[];foreach((array)$existing['baseline_usable_topic_keys'] as $key){$key=trim((string)$key);if($key!=='')$set[$key]=true;}
+        $keys=array_keys($set);sort($keys,SORT_STRING);
+        return ['queue_uuid'=>$queueUuid,'baseline_usable_topic_keys'=>$keys];
+    }
+
+
     /** Current strict PASS topics minus the wave-start PASS baseline. No title words or discovery history define identity. */
     private static function countNewUniqueUsableTopics(array $queue,string $runUuid=''): array {
         $baseline=[];foreach((array)($queue['baseline_usable_topic_keys']??[]) as $key){$key=trim((string)$key);if($key!=='')$baseline[$key]=true;}
@@ -479,7 +514,7 @@
         $m=self::cancelMarker();$cancel=is_array($m)&&hash_equals((string)($q['queue_uuid']??''),(string)($m['queue_uuid']??''));
         $mode=(string)($q['mode']??'FIXED_FAMILY_COUNT');$target=(int)($q['target_usable']??0);$usable=(int)($q['usable_candidate_count']??0);
-        return ['contract'=>self::CONTRACT,'queue_uuid'=>(string)$q['queue_uuid'],'status'=>(string)$q['status'],'mode'=>$mode,'requested_count'=>(int)$q['requested_count'],'target_usable'=>$target,'usable_candidate_count'=>$usable,'raw_usable_candidate_count'=>(int)($q['raw_usable_candidate_count']??$usable),'target_achieved'=>$mode==='TARGET_USABLE_CANDIDATES'&&$target>0&&$usable>=$target,'max_items'=>(int)($q['max_items']??($q['item_count']??self::LEGACY_MAX_ITEMS)),'completion_reason'=>(string)($q['completion_reason']??''),'local_backlog_complete'=>!empty($q['local_backlog_complete']),'local_backlog_reused'=>!empty($q['local_backlog_reused']),'local_backlog_resumed'=>!empty($q['local_backlog_resumed']),'transport_recovery_mode'=>self::transportRecoveryMode($q,$child),'local_backlog_cursor'=>(int)($q['local_backlog_cursor']??0),'local_backlog_processed'=>(int)($q['local_backlog_processed']??0),'local_backlog_promoted'=>(int)($q['local_backlog_promoted']??0),'local_backlog_provider_calls'=>(int)($q['local_backlog_provider_calls']??0),'item_count'=>(int)$q['item_count'],'current_index'=>(int)$q['current_index'],'completed_count'=>(int)$q['completed_count'],'blocked_count'=>(int)($q['blocked_count']??0),'last_error'=>(string)$q['last_error'],'coordination_wait_code'=>(string)($q['coordination_wait_code']??''),'cancel_requested'=>$cancel,'cancel_requested_at_utc'=>$cancel?(string)($m['requested_at_utc']??''):'','cancelled_at_utc'=>(string)($q['cancelled_at_utc']??''),
+        return ['contract'=>self::CONTRACT,'queue_uuid'=>(string)$q['queue_uuid'],'status'=>(string)$q['status'],'mode'=>$mode,'requested_count'=>(int)$q['requested_count'],'target_usable'=>$target,'usable_candidate_count'=>$usable,'raw_usable_candidate_count'=>(int)($q['raw_usable_candidate_count']??$usable),'target_achieved'=>$mode==='TARGET_USABLE_CANDIDATES'&&$target>0&&$usable>=$target,'max_items'=>(int)($q['max_items']??($q['item_count']??self::LEGACY_MAX_ITEMS)),'completion_reason'=>(string)($q['completion_reason']??''),'continued_from_queue_uuid'=>(string)($q['continued_from_queue_uuid']??''),'continued_usable_candidate_count'=>(int)($q['continued_usable_candidate_count']??0),'local_backlog_complete'=>!empty($q['local_backlog_complete']),'local_backlog_reused'=>!empty($q['local_backlog_reused']),'local_backlog_resumed'=>!empty($q['local_backlog_resumed']),'transport_recovery_mode'=>self::transportRecoveryMode($q,$child),'local_backlog_cursor'=>(int)($q['local_backlog_cursor']??0),'local_backlog_processed'=>(int)($q['local_backlog_processed']??0),'local_backlog_promoted'=>(int)($q['local_backlog_promoted']??0),'local_backlog_provider_calls'=>(int)($q['local_backlog_provider_calls']??0),'item_count'=>(int)$q['item_count'],'current_index'=>(int)$q['current_index'],'completed_count'=>(int)$q['completed_count'],'blocked_count'=>(int)($q['blocked_count']??0),'last_error'=>(string)$q['last_error'],'coordination_wait_code'=>(string)($q['coordination_wait_code']??''),'cancel_requested'=>$cancel,'cancel_requested_at_utc'=>$cancel?(string)($m['requested_at_utc']??''):'','cancelled_at_utc'=>(string)($q['cancelled_at_utc']??''),
             'items'=>array_map(static fn($i)=>['category_id'=>(int)$i['category_id'],'family'=>(string)$i['family'],'root'=>(string)$i['root'],'status'=>(string)$i['status'],'actual_cost'=>(float)$i['actual_cost'],'usable_candidate_count'=>(int)($i['usable_candidate_count']??0),'raw_usable_candidate_count'=>(int)($i['raw_usable_candidate_count']??($i['usable_candidate_count']??0)),'candidate_count'=>(int)($i['candidate_count']??0),'count_status'=>(string)($i['count_status']??''),'last_error'=>(string)$i['last_error'],'coordination_wait_code'=>(string)($i['coordination_wait_code']??''),'child_job_uuid'=>(string)($i['child_job_uuid']??''),'parked_job_uuid'=>(string)($i['parked_job_uuid']??''),'blocked_at_utc'=>(string)($i['blocked_at_utc']??'')],(array)$q['items']),'child'=>$child,'integrity_error'=>$integrityError,'foreign_active_job'=>is_array($foreignChild)?['job_uuid'=>(string)($foreignChild['job_uuid']??''),'status'=>(string)($foreignChild['status']??''),'category_id'=>(int)($foreignChild['category_id']??0),'seed'=>(string)($foreignChild['seed']??''),'owner_kind'=>(string)($foreignChild['owner_kind']??''),'owner_queue_uuid'=>(string)($foreignChild['owner_queue_uuid']??''),'owner_item_index'=>(int)($foreignChild['owner_item_index']??-1)]:null,'cancel_resolution'=>(string)($q['cancel_resolution']??''),'updated_at_utc'=>(string)$q['updated_at_utc']];
     }
--- a/includes/class-pste-admin.php
+++ b/includes/class-pste-admin.php
@@ -354,7 +354,7 @@
         $breadthStatus=(string)($breadth['status']??'');$breadthActive=is_array($breadth)&&in_array($breadthStatus,['RUNNING','PAUSED_ERROR','OUTCOME_UNKNOWN'],true);$breadthDisplay=$breadthActive?'block':'none';$startDisplay=$breadthActive?'none':'block';
         echo '<div class="pste-box"><strong>Server-Driver:</strong> <span id="pste-driver-status">'.esc_html((string)($driver['status']??'IDLE')).'</span> · <span id="pste-driver-health">'.esc_html((string)($driver['health_code']??'')).'</span><br><span id="pste-driver-message">'.esc_html((string)($driver['message']??'')).'</span><br><small>Schritt <span id="pste-driver-step-number">'.esc_html((string)($driver['step_number']??0)).'</span> · <span id="pste-driver-last-step">'.esc_html((string)($driver['last_step']??'')).'</span> · letzter Fortschritt <span id="pste-driver-last-progress">'.esc_html((string)($driver['last_progress_at_utc']??'')).'</span></small></div>';
-        echo '<div class="pste-box" id="pste-active-breadth" style="display:'.esc_attr($breadthDisplay).'"><p><strong>Produktionswelle:</strong> <span id="pste-breadth-status">'.esc_html($breadthStatus!==''?$breadthStatus:'RUNNING').'</span> · neue eindeutige PASS-Themen <strong><span id="pste-breadth-usable">'.esc_html((string)($breadth['usable_candidate_count']??0)).'</span> / <span id="pste-breadth-target-display">'.esc_html((string)($breadth['target_usable']??40)).'</span></strong> · Familien <span id="pste-breadth-progress">'.esc_html((string)($breadth['completed_count']??0)).' von max. '.esc_html((string)($breadth['max_items']??$breadth['item_count']??40)).'</span> · geparkte blockierte Schritte <span id="pste-breadth-blocked">'.esc_html((string)($breadth['blocked_count']??0)).'</span></p><p id="pste-breadth-detail"></p><p><small>Aktiver Serverzustand: <span id="pste-breadth-live-health">'.esc_html((string)($driver['health_code']??'')).'</span></small></p>';
+        echo '<div class="pste-box" id="pste-active-breadth" style="display:'.esc_attr($breadthDisplay).'"><p><strong>Produktionswelle:</strong> <span id="pste-breadth-status">'.esc_html($breadthStatus!==''?$breadthStatus:'RUNNING').'</span> · PASS-Themen seit Wellenbeginn <strong><span id="pste-breadth-usable">'.esc_html((string)($breadth['usable_candidate_count']??0)).'</span> / <span id="pste-breadth-target-display">'.esc_html((string)($breadth['target_usable']??40)).'</span></strong> · Familien <span id="pste-breadth-progress">'.esc_html((string)($breadth['completed_count']??0)).' von max. '.esc_html((string)($breadth['max_items']??$breadth['item_count']??40)).'</span> · geparkte blockierte Schritte <span id="pste-breadth-blocked">'.esc_html((string)($breadth['blocked_count']??0)).'</span></p><p id="pste-breadth-detail"></p><p><small>Aktiver Serverzustand: <span id="pste-breadth-live-health">'.esc_html((string)($driver['health_code']??'')).'</span></small></p>';
@@ -374,7 +374,7 @@
-        echo '<script>(function(){"use strict";const cfg='.$config.';let job=cfg.active||null,queue=cfg.breadth||null,driver=cfg.driver||{};const activeStates=["RUNNING","PAUSED_ERROR","OUTCOME_UNKNOWN"],feedback=document.getElementById("pste-research-feedback"),startBox=document.getElementById("pste-breadth-start-box"),activeBox=document.getElementById("pste-active-breadth");if(queue&&!activeStates.includes(String(queue.status||"")))queue=null;function msg(t,e){if(!feedback)return;feedback.style.display="block";feedback.className="notice "+(e?"notice-error":"notice-info")+" inline";feedback.querySelector("p").textContent=t;}function setBreadthActive(on){if(startBox)startBox.style.display=on?"none":"block";if(activeBox)activeBox.style.display=on?"block":"none";}function ageText(v){if(!v)return "noch kein Fortschritt";const t=Date.parse(v);if(!Number.isFinite(t))return String(v);const sec=Math.max(0,Math.floor((Date.now()-t)/1000));return sec<60?"vor "+sec+" s":"vor "+Math.floor(sec/60)+" min "+(sec%60)+" s";}async function call(action,data){const body=new URLSearchParams(Object.assign({action,nonce:cfg.nonce},data||{}));const r=await fetch(cfg.ajax_url,{method:"POST",credentials:"same-origin",headers:{"Content-Type":"application/x-www-form-urlencoded;charset=UTF-8","Accept":"application/json"},body:body.toString()});const raw=await r.text();let j;try{j=JSON.parse((raw||"").replace(/^\uFEFF/,"").trim());}catch(e){throw new Error("PSTE_AJAX_INVALID_JSON_HTTP_"+r.status);}if(!r.ok||!j.success)throw new Error((j.data&&j.data.error_code)||("PSTE_AJAX_REQUEST_FAILED_HTTP_"+r.status));return j.data;}function renderDriver(d){driver=d||driver;const s=document.getElementById("pste-driver-status"),h=document.getElementById("pste-driver-health"),m=document.getElementById("pste-driver-message"),n=document.getElementById("pste-driver-step-number"),ls=document.getElementById("pste-driver-last-step"),lp=document.getElementById("pste-driver-last-progress"),bh=document.getElementById("pste-breadth-live-health");if(s)s.textContent=driver.status||"IDLE";if(h)h.textContent=driver.health_code||"";if(m)m.textContent=driver.message||"";if(n)n.textContent=String(driver.step_number||0);if(ls)ls.textContent=driver.last_step||"";if(lp)lp.textContent=ageText(driver.last_progress_at_utc||driver.step_started_at_utc||"");if(bh){const timing=Number(driver.active_step_age_seconds||0)>0?"aktiver Schritt seit "+Number(driver.active_step_age_seconds||0)+" s":"letzter Schritt "+Number(driver.last_step_duration_ms||0)+" ms";bh.textContent=(driver.status||"IDLE")+" · "+(driver.health_code||"")+" · "+timing+" · "+ageText(driver.last_progress_at_utc||driver.step_started_at_utc||"");}if(driver.status==="BLOCKED"||driver.health_code==="PSTE_DRIVER_STEP_OVERDUE"||driver.health_code==="PSTE_DRIVER_WAKEUP_OVERDUE")msg("SYSTEMSTATUS: HÄNGT-BLOCKED · "+(driver.last_error||driver.health_code||"PSTE_DRIVER_BLOCKED")+" · Stand ist gespeichert.",true);}function jobText(j){if(!j)return "";let x="Datenquellen "+Number(j.progress||0)+" von "+Number(j.total_provider_steps||0)+" · Fragen "+String(j.questions_state||"NOT_STARTED");if(j.phase==="FINALIZE"){const st=String(j.finalize_stage||"PREPARE");x+=" · Abschluss "+st;if(st==="PREPARE_CANDIDATES")x+=" "+Number(j.finalize_prepare_cursor||0)+"/"+Number(j.finalize_prepare_total||0);if(st==="PREPARE_COMMIT")x+=" "+Number(j.finalize_prepare_commit_cursor||0)+"/"+Number(j.finalize_total||0);if(st==="SANDBOX_BATCH")x+=" "+Number(j.finalize_sandbox_cursor||0)+"/"+Number(j.finalize_total||0);if(st==="CONTEXT_BATCH")x+=" "+Number(j.finalize_context_cursor||0)+"/"+Number(j.finalize_total||0);}return x;}function renderJob(j){job=j||job;if(!job)return;const s=document.getElementById("pste-job-status"),p=document.getElementById("pste-job-progress"),c=document.getElementById("pste-job-cost"),d=document.getElementById("pste-job-detail");if(s)s.textContent=job.status||"";if(p)p.textContent=jobText(job);if(c)c.textContent=Number(job.actual_cost||0).toFixed(6)+" USD";if(d)d.textContent=(job.last_error?"Fehler: "+job.last_error+" · ":"")+"Server-Driver entscheidet den nächsten Schritt.";}function renderQueue(q){queue=q||queue;if(!queue)return;setBreadthActive(true);const s=document.getElementById("pste-breadth-status"),p=document.getElementById("pste-breadth-progress"),u=document.getElementById("pste-breadth-usable"),tgt=document.getElementById("pste-breadth-target-display"),b=document.getElementById("pste-breadth-blocked"),d=document.getElementById("pste-breadth-detail"),resume=document.getElementById("pste-breadth-resume");if(s)s.textContent=queue.status||"";if(p)p.textContent=Number(queue.completed_count||0)+" von max. "+Number(queue.max_items||queue.item_count||0);if(u)u.textContent=Number(queue.usable_candidate_count||0);if(tgt)tgt.textContent=Number(queue.target_usable||0);if(b)b.textContent=Number(queue.blocked_count||0);if(resume)resume.style.display=["PAUSED_ERROR","OUTCOME_UNKNOWN"].includes(String(queue.status||""))?"inline-block":"none";const cur=(queue.items||[])[Number(queue.current_index||0)],child=queue.child||null;let text=queue.local_backlog_reused?"Lokaler Bestand: vollständige Prüfung wiederverwendet.":(!queue.local_backlog_complete?"Lokaler Bestand: "+Number(queue.local_backlog_processed||0)+" geprüft.":"");if(cur)text+=(text?" ":"")+"Aktuell: "+String(cur.family||"")+".";if(child)text+=" Kindlauf: "+jobText(child)+".";if(queue.last_blocked_error)text+=" Zuletzt geparkt: "+queue.last_blocked_error+".";if(d)d.textContent=text;}async function refresh(){
+        echo '<script>(function(){"use strict";const cfg='.$config.';let job=cfg.active||null,queue=cfg.breadth||null,driver=cfg.driver||{};const activeStates=["RUNNING","PAUSED_ERROR","OUTCOME_UNKNOWN"],feedback=document.getElementById("pste-research-feedback"),startBox=document.getElementById("pste-breadth-start-box"),activeBox=document.getElementById("pste-active-breadth");if(queue&&!activeStates.includes(String(queue.status||"")))queue=null;function msg(t,e){if(!feedback)return;feedback.style.display="block";feedback.className="notice "+(e?"notice-error":"notice-info")+" inline";feedback.querySelector("p").textContent=t;}function setBreadthActive(on){if(startBox)startBox.style.display=on?"none":"block";if(activeBox)activeBox.style.display=on?"block":"none";}function ageText(v){if(!v)return "noch kein Fortschritt";const t=Date.parse(v);if(!Number.isFinite(t))return String(v);const sec=Math.max(0,Math.floor((Date.now()-t)/1000));return sec<60?"vor "+sec+" s":"vor "+Math.floor(sec/60)+" min "+(sec%60)+" s";}async function call(action,data){const body=new URLSearchParams(Object.assign({action,nonce:cfg.nonce},data||{}));const r=await fetch(cfg.ajax_url,{method:"POST",credentials:"same-origin",headers:{"Content-Type":"application/x-www-form-urlencoded;charset=UTF-8","Accept":"application/json"},body:body.toString()});const raw=await r.text();let j;try{j=JSON.parse((raw||"").replace(/^\uFEFF/,"").trim());}catch(e){throw new Error("PSTE_AJAX_INVALID_JSON_HTTP_"+r.status);}if(!r.ok||!j.success)throw new Error((j.data&&j.data.error_code)||("PSTE_AJAX_REQUEST_FAILED_HTTP_"+r.status));return j.data;}function renderDriver(d){driver=d||driver;const s=document.getElementById("pste-driver-status"),h=document.getElementById("pste-driver-health"),m=document.getElementById("pste-driver-message"),n=document.getElementById("pste-driver-step-number"),ls=document.getElementById("pste-driver-last-step"),lp=document.getElementById("pste-driver-last-progress"),bh=document.getElementById("pste-breadth-live-health");if(s)s.textContent=driver.status||"IDLE";if(h)h.textContent=driver.health_code||"";if(m)m.textContent=driver.message||"";if(n)n.textContent=String(driver.step_number||0);if(ls)ls.textContent=driver.last_step||"";if(lp)lp.textContent=ageText(driver.last_progress_at_utc||driver.step_started_at_utc||"");if(bh){const timing=Number(driver.active_step_age_seconds||0)>0?"aktiver Schritt seit "+Number(driver.active_step_age_seconds||0)+" s":"letzter Schritt "+Number(driver.last_step_duration_ms||0)+" ms";bh.textContent=(driver.status||"IDLE")+" · "+(driver.health_code||"")+" · "+timing+" · "+ageText(driver.last_progress_at_utc||driver.step_started_at_utc||"");}if(driver.status==="BLOCKED"||driver.health_code==="PSTE_DRIVER_STEP_OVERDUE"||driver.health_code==="PSTE_DRIVER_WAKEUP_OVERDUE")msg("SYSTEMSTATUS: HÄNGT-BLOCKED · "+(driver.last_error||driver.health_code||"PSTE_DRIVER_BLOCKED")+" · Stand ist gespeichert.",true);}function jobText(j){if(!j)return "";let x="Datenquellen "+Number(j.progress||0)+" von "+Number(j.total_provider_steps||0)+" · Fragen "+String(j.questions_state||"NOT_STARTED");if(j.phase==="FINALIZE"){const st=String(j.finalize_stage||"PREPARE");x+=" · Abschluss "+st;if(st==="PREPARE_CANDIDATES")x+=" "+Number(j.finalize_prepare_cursor||0)+"/"+Number(j.finalize_prepare_total||0);if(st==="PREPARE_COMMIT")x+=" "+Number(j.finalize_prepare_commit_cursor||0)+"/"+Number(j.finalize_total||0);if(st==="SANDBOX_BATCH")x+=" "+Number(j.finalize_sandbox_cursor||0)+"/"+Number(j.finalize_total||0);if(st==="CONTEXT_BATCH")x+=" "+Number(j.finalize_context_cursor||0)+"/"+Number(j.finalize_total||0);}return x;}function renderJob(j){job=j||job;if(!job)return;const s=document.getElementById("pste-job-status"),p=document.getElementById("pste-job-progress"),c=document.getElementById("pste-job-cost"),d=document.getElementById("pste-job-detail");if(s)s.textContent=job.status||"";if(p)p.textContent=jobText(job);if(c)c.textContent=Number(job.actual_cost||0).toFixed(6)+" USD";if(d)d.textContent=(job.last_error?"Fehler: "+job.last_error+" · ":"")+"Server-Driver entscheidet den nächsten Schritt.";}function renderQueue(q){queue=q||queue;if(!queue)return;setBreadthActive(true);const s=document.getElementById("pste-breadth-status"),p=document.getElementById("pste-breadth-progress"),u=document.getElementById("pste-breadth-usable"),tgt=document.getElementById("pste-breadth-target-display"),b=document.getElementById("pste-breadth-blocked"),d=document.getElementById("pste-breadth-detail"),resume=document.getElementById("pste-breadth-resume");if(s)s.textContent=queue.status||"";if(p)p.textContent=Number(queue.completed_count||0)+" von max. "+Number(queue.max_items||queue.item_count||0);if(u)u.textContent=Number(queue.usable_candidate_count||0);if(tgt)tgt.textContent=Number(queue.target_usable||0);if(b)b.textContent=Number(queue.blocked_count||0);if(resume)resume.style.display=["PAUSED_ERROR","OUTCOME_UNKNOWN"].includes(String(queue.status||""))?"inline-block":"none";const cur=(queue.items||[])[Number(queue.current_index||0)],child=queue.child||null;let text=Number(queue.continued_usable_candidate_count||0)>0?"Fortsetzung: "+Number(queue.continued_usable_candidate_count||0)+" bereits erreichte PASS-Themen übernommen. ":"";text+=queue.local_backlog_reused?"Lokaler Bestand: vollständige Prüfung wiederverwendet.":(!queue.local_backlog_complete?"Lokaler Bestand: "+Number(queue.local_backlog_processed||0)+" geprüft.":"");if(cur)text+=(text?" ":"")+"Aktuell: "+String(cur.family||"")+".";if(child)text+=" Kindlauf: "+jobText(child)+".";if(queue.last_blocked_error)text+=" Zuletzt geparkt: "+queue.last_blocked_error+".";if(d)d.textContent=text;}async function refresh(){
