--- a/includes/class-pste-admin.php
+++ b/includes/class-pste-admin.php
@@ -373,7 +373,7 @@
         }
         echo '<div id="pste-research-feedback" class="notice notice-info inline" style="display:none"><p></p></div>';
         $config=wp_json_encode(['ajax_url'=>admin_url('admin-ajax.php'),'nonce'=>$nonce,'active'=>$active,'breadth'=>$breadth,'driver'=>$driver],JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES);
-        echo '<script>(function(){"use strict";const cfg='.$config.';let job=cfg.active||null,queue=cfg.breadth||null,driver=cfg.driver||{};const feedback=document.getElementById("pste-research-feedback");function msg(t,e){if(!feedback)return;feedback.style.display="block";feedback.className="notice "+(e?"notice-error":"notice-info")+" inline";feedback.querySelector("p").textContent=t;}async function call(action,data){const body=new URLSearchParams(Object.assign({action,nonce:cfg.nonce},data||{}));const r=await fetch(cfg.ajax_url,{method:"POST",credentials:"same-origin",headers:{"Content-Type":"application/x-www-form-urlencoded;charset=UTF-8","Accept":"application/json"},body:body.toString()});const raw=await r.text();let j;try{j=JSON.parse((raw||"").replace(/^\\uFEFF/,"").trim());}catch(e){throw new Error("PSTE_AJAX_INVALID_JSON_HTTP_"+r.status);}if(!r.ok||!j.success)throw new Error((j.data&&j.data.error_code)||("PSTE_AJAX_REQUEST_FAILED_HTTP_"+r.status));return j.data;}function renderDriver(d){driver=d||driver;const s=document.getElementById("pste-driver-status"),h=document.getElementById("pste-driver-health"),m=document.getElementById("pste-driver-message");if(s)s.textContent=driver.status||"IDLE";if(h)h.textContent=driver.health_code||"";if(m)m.textContent=driver.message||"";if(driver.status==="BLOCKED"||driver.health==="STEP_OVERDUE"||driver.health==="WAKEUP_OVERDUE")msg("SYSTEMSTATUS: HÄNGT-BLOCKED · "+(driver.last_error||driver.health_code||"PSTE_DRIVER_BLOCKED")+" · Stand ist gespeichert.",true);}function jobText(j){if(!j)return "";let x="Datenquellen "+Number(j.progress||0)+" von "+Number(j.total_provider_steps||0)+" · Fragen "+String(j.questions_state||"NOT_STARTED");if(j.phase==="FINALIZE")x+=" · Abschluss "+String(j.finalize_stage||"PREPARE");return x;}function renderJob(j){job=j||job;if(!job)return;const s=document.getElementById("pste-job-status"),p=document.getElementById("pste-job-progress"),c=document.getElementById("pste-job-cost"),d=document.getElementById("pste-job-detail");if(s)s.textContent=job.status||"";if(p)p.textContent=jobText(job);if(c)c.textContent=Number(job.actual_cost||0).toFixed(6)+" USD";if(d)d.textContent=(job.last_error?"Fehler: "+job.last_error+" · ":"")+"Server-Driver entscheidet den nächsten Schritt.";}function renderQueue(q){queue=q||queue;if(!queue)return;const s=document.getElementById("pste-breadth-status"),p=document.getElementById("pste-breadth-progress"),u=document.getElementById("pste-breadth-usable"),b=document.getElementById("pste-breadth-blocked"),d=document.getElementById("pste-breadth-detail");if(s)s.textContent=queue.status||"";if(p)p.textContent=Number(queue.completed_count||0)+" von max. "+Number(queue.max_items||queue.item_count||0);if(u)u.textContent=Number(queue.usable_candidate_count||0);if(b)b.textContent=Number(queue.blocked_count||0);const cur=(queue.items||[])[Number(queue.current_index||0)],child=queue.child||null;let t=queue.local_backlog_reused?"Lokaler Bestand: vollständige Prüfung wiederverwendet.":(!queue.local_backlog_complete?"Lokaler Bestand: "+Number(queue.local_backlog_processed||0)+" geprüft.":"");if(cur)t+=" Aktuell: "+String(cur.family||"")+".";if(child)t+=" Kindlauf: "+jobText(child)+".";if(queue.last_blocked_error)t+=" Zuletzt geparkt: "+queue.last_blocked_error+".";if(d)d.textContent=t;}async function refresh(){try{if(queue&&queue.queue_uuid){const d=await call("pste_breadth_research_status",{queue_uuid:queue.queue_uuid});renderQueue(d.queue);renderDriver(d.driver);if(["COMPLETE","CANCELLED"].includes(String(d.queue.status||""))){msg("Produktionswelle "+d.queue.status+" · "+String(d.queue.completion_reason||""),false);return;}}else if(job&&job.job_uuid){const d=await call("pste_research_status",{job_uuid:job.job_uuid});if(d.job)renderJob(d.job);renderDriver(d.driver);if(["COMPLETE","CANCELLED"].includes(String((d.driver||{}).status||"")))msg("Einzellauf "+d.driver.status+".",false);}}catch(e){msg(e.message||"PSTE_STATUS_READ_FAILED",true);}}const bs=document.getElementById("pste-breadth-start");if(bs)bs.addEventListener("click",async()=>{try{const c=document.getElementById("pste-breadth-confirm-cost");if(!c||!c.checked)throw new Error("Bitte Kosten bestätigen.");const d=await call("pste_breadth_research_start",{target_usable:document.getElementById("pste-breadth-target").value,max_items:document.getElementById("pste-breadth-max").value,confirm_cost:"1"});queue=d.queue;job=null;renderQueue(queue);renderDriver(d.driver);msg("Server-Driver gestartet. Der Browser steuert keine Arbeitsschritte.",false);}catch(e){msg(e.message,true);}});const br=document.getElementById("pste-breadth-resume");if(br)br.addEventListener("click",async()=>{try{const d=await call("pste_breadth_research_advance",{queue_uuid:queue.queue_uuid,explicit_resume:"1"});renderQueue(d.queue);renderDriver(d.driver);msg("Gespeicherter Block zur erneuten serverseitigen Prüfung freigegeben.",false);}catch(e){msg(e.message,true);}});const bc=document.getElementById("pste-breadth-cancel");if(bc)bc.addEventListener("click",async()=>{if(!confirm("Produktionswelle sicher abbrechen? Bereits gespeicherte Daten bleiben erhalten."))return;try{const d=await call("pste_breadth_research_cancel",{queue_uuid:queue.queue_uuid});renderQueue(d.queue);renderDriver(d.driver||driver);msg("Abbruch gespeichert. Bereits recherchierte Daten bleiben erhalten.",false);}catch(e){msg(e.message,true);}});const f=document.getElementById("pste-run-form");if(f){const sel=document.getElementById("pste-category-select"),seed=document.getElementById("pste-seed");sel.addEventListener("change",()=>{const o=sel.selectedOptions[0];if(o)seed.value=o.dataset.seed||"";});f.addEventListener("submit",async e=>{e.preventDefault();try{const d=await call("pste_research_start",{category_id:sel.value,seed:seed.value,confirm_cost:document.getElementById("pste-confirm-cost").checked?"1":"0"});job=d.job;queue=null;renderJob(job);renderDriver(d.driver);msg("Einzellauf an den Server-Driver übergeben.",false);}catch(x){msg(x.message,true);}});}const rr=document.getElementById("pste-resume-research");if(rr)rr.addEventListener("click",async()=>{try{const d=await call("pste_research_advance",{job_uuid:job.job_uuid,explicit_resume:"1"});if(d.job)renderJob(d.job);renderDriver(d.driver);msg("Gespeicherter Block zur erneuten serverseitigen Prüfung freigegeben.",false);}catch(e){msg(e.message,true);}});const rc=document.getElementById("pste-cancel-research");if(rc)rc.addEventListener("click",async()=>{try{const ck=document.getElementById("pste-provider-checked");if(!ck||!ck.checked)throw new Error("Providerprüfung muss bestätigt werden.");const d=await call("pste_research_cancel",{job_uuid:job.job_uuid,provider_checked:"1"});renderDriver(d.driver||driver);msg("Unklarer Lauf beendet; Archiv bleibt erhalten.",false);}catch(e){msg(e.message,true);}});renderDriver(driver);if(queue)renderQueue(queue);if(job&&!queue)renderJob(job);setInterval(refresh,2000);refresh();})();</script>';
+        echo '<script>(function(){"use strict";const cfg='.$config.';let job=cfg.active||null,queue=cfg.breadth||null,driver=cfg.driver||{};const feedback=document.getElementById("pste-research-feedback");function msg(t,e){if(!feedback)return;feedback.style.display="block";feedback.className="notice "+(e?"notice-error":"notice-info")+" inline";feedback.querySelector("p").textContent=t;}async function call(action,data){const body=new URLSearchParams(Object.assign({action,nonce:cfg.nonce},data||{}));const r=await fetch(cfg.ajax_url,{method:"POST",credentials:"same-origin",headers:{"Content-Type":"application/x-www-form-urlencoded;charset=UTF-8","Accept":"application/json"},body:body.toString()});const raw=await r.text();let j;try{j=JSON.parse((raw||"").replace(/^\\uFEFF/,"").trim());}catch(e){throw new Error("PSTE_AJAX_INVALID_JSON_HTTP_"+r.status);}if(!r.ok||!j.success)throw new Error((j.data&&j.data.error_code)||("PSTE_AJAX_REQUEST_FAILED_HTTP_"+r.status));return j.data;}function renderDriver(d){driver=d||driver;const s=document.getElementById("pste-driver-status"),h=document.getElementById("pste-driver-health"),m=document.getElementById("pste-driver-message");if(s)s.textContent=driver.status||"IDLE";if(h)h.textContent=driver.health_code||"";if(m)m.textContent=driver.message||"";if(driver.status==="BLOCKED"||driver.health==="STEP_OVERDUE"||driver.health==="WAKEUP_OVERDUE")msg("SYSTEMSTATUS: HÄNGT-BLOCKED · "+(driver.last_error||driver.health_code||"PSTE_DRIVER_BLOCKED")+" · Stand ist gespeichert.",true);}function jobText(j){if(!j)return "";let x="Datenquellen "+Number(j.progress||0)+" von "+Number(j.total_provider_steps||0)+" · Fragen "+String(j.questions_state||"NOT_STARTED");if(j.phase==="FINALIZE")x+=" · Abschluss "+String(j.finalize_stage||"PREPARE");return x;}function renderJob(j){job=j||job;if(!job)return;const s=document.getElementById("pste-job-status"),p=document.getElementById("pste-job-progress"),c=document.getElementById("pste-job-cost"),d=document.getElementById("pste-job-detail");if(s)s.textContent=job.status||"";if(p)p.textContent=jobText(job);if(c)c.textContent=Number(job.actual_cost||0).toFixed(6)+" USD";if(d)d.textContent=(job.last_error?"Fehler: "+job.last_error+" · ":"")+"Server-Driver entscheidet den nächsten Schritt.";}function renderQueue(q){queue=q||queue;if(!queue)return;const s=document.getElementById("pste-breadth-status"),p=document.getElementById("pste-breadth-progress"),u=document.getElementById("pste-breadth-usable"),b=document.getElementById("pste-breadth-blocked"),d=document.getElementById("pste-breadth-detail");if(s)s.textContent=queue.status||"";if(p)p.textContent=Number(queue.completed_count||0)+" von max. "+Number(queue.max_items||queue.item_count||0);if(u)u.textContent=Number(queue.usable_candidate_count||0);if(b)b.textContent=Number(queue.blocked_count||0);const cur=(queue.items||[])[Number(queue.current_index||0)],child=queue.child||null;let t=queue.local_backlog_reused?"Lokaler Bestand: vollständige Prüfung wiederverwendet.":(!queue.local_backlog_complete?"Lokaler Bestand: "+Number(queue.local_backlog_processed||0)+" geprüft.":"");if(cur)t+=" Aktuell: "+String(cur.family||"")+".";if(child)t+=" Kindlauf: "+jobText(child)+".";if(queue.last_blocked_error)t+=" Zuletzt geparkt: "+queue.last_blocked_error+".";if(d)d.textContent=t;}async function refresh(){let requestedQueueUuid="";try{if(queue&&queue.queue_uuid){requestedQueueUuid=String(queue.queue_uuid||"");const d=await call("pste_breadth_research_status",{queue_uuid:requestedQueueUuid});if(!queue||String(queue.queue_uuid||"")!==requestedQueueUuid)return;renderQueue(d.queue);renderDriver(d.driver);if(["COMPLETE","CANCELLED"].includes(String(d.queue.status||""))){msg("Produktionswelle "+d.queue.status+" · "+String(d.queue.completion_reason||""),false);return;}}else if(job&&job.job_uuid){const d=await call("pste_research_status",{job_uuid:job.job_uuid});if(d.job)renderJob(d.job);renderDriver(d.driver);if(["COMPLETE","CANCELLED"].includes(String((d.driver||{}).status||"")))msg("Einzellauf "+d.driver.status+".",false);}}catch(e){if(requestedQueueUuid&&queue&&String(queue.queue_uuid||"")!==requestedQueueUuid)return;msg(e.message||"PSTE_STATUS_READ_FAILED",true);}}const bs=document.getElementById("pste-breadth-start");if(bs)bs.addEventListener("click",async()=>{try{const c=document.getElementById("pste-breadth-confirm-cost");if(!c||!c.checked)throw new Error("Bitte Kosten bestätigen.");const d=await call("pste_breadth_research_start",{target_usable:document.getElementById("pste-breadth-target").value,max_items:document.getElementById("pste-breadth-max").value,confirm_cost:"1"});queue=d.queue;job=null;renderQueue(queue);renderDriver(d.driver);msg("Server-Driver gestartet. Der Browser steuert keine Arbeitsschritte.",false);}catch(e){msg(e.message,true);}});const br=document.getElementById("pste-breadth-resume");if(br)br.addEventListener("click",async()=>{try{const d=await call("pste_breadth_research_advance",{queue_uuid:queue.queue_uuid,explicit_resume:"1"});renderQueue(d.queue);renderDriver(d.driver);msg("Gespeicherter Block zur erneuten serverseitigen Prüfung freigegeben.",false);}catch(e){msg(e.message,true);}});const bc=document.getElementById("pste-breadth-cancel");if(bc)bc.addEventListener("click",async()=>{if(!confirm("Produktionswelle sicher abbrechen? Bereits gespeicherte Daten bleiben erhalten."))return;try{const d=await call("pste_breadth_research_cancel",{queue_uuid:queue.queue_uuid});renderQueue(d.queue);renderDriver(d.driver||driver);msg("Abbruch gespeichert. Bereits recherchierte Daten bleiben erhalten.",false);}catch(e){msg(e.message,true);}});const f=document.getElementById("pste-run-form");if(f){const sel=document.getElementById("pste-category-select"),seed=document.getElementById("pste-seed");sel.addEventListener("change",()=>{const o=sel.selectedOptions[0];if(o)seed.value=o.dataset.seed||"";});f.addEventListener("submit",async e=>{e.preventDefault();try{const d=await call("pste_research_start",{category_id:sel.value,seed:seed.value,confirm_cost:document.getElementById("pste-confirm-cost").checked?"1":"0"});job=d.job;queue=null;renderJob(job);renderDriver(d.driver);msg("Einzellauf an den Server-Driver übergeben.",false);}catch(x){msg(x.message,true);}});}const rr=document.getElementById("pste-resume-research");if(rr)rr.addEventListener("click",async()=>{try{const d=await call("pste_research_advance",{job_uuid:job.job_uuid,explicit_resume:"1"});if(d.job)renderJob(d.job);renderDriver(d.driver);msg("Gespeicherter Block zur erneuten serverseitigen Prüfung freigegeben.",false);}catch(e){msg(e.message,true);}});const rc=document.getElementById("pste-cancel-research");if(rc)rc.addEventListener("click",async()=>{try{const ck=document.getElementById("pste-provider-checked");if(!ck||!ck.checked)throw new Error("Providerprüfung muss bestätigt werden.");const d=await call("pste_research_cancel",{job_uuid:job.job_uuid,provider_checked:"1"});renderDriver(d.driver||driver);msg("Unklarer Lauf beendet; Archiv bleibt erhalten.",false);}catch(e){msg(e.message,true);}});renderDriver(driver);if(queue)renderQueue(queue);if(job&&!queue)renderJob(job);setInterval(refresh,2000);refresh();})();</script>';
         if($latest){echo '<p><strong>Letzter abgeschlossener Lauf:</strong> '.esc_html((string)$latest['status']).' · '.esc_html((string)($latest['payload']['usable_candidate_count']??0)).' direkt nutzbare Themen.</p>';self::actionForm('pste_export_overlay','pste_export_overlay','Letztes Overlay exportieren');self::actionForm('pste_export_topic_map','pste_export_topic_map','Gesamte Themenkarte exportieren');}
     }
