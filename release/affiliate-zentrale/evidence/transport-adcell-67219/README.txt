ADCELL 6.72.19 byte-exact transport staging only.

This directory is NOT release authority and does not change canonical source.
Upload only the nine exact files from ADCELL_6.72.19_GITHUB_TRANSPORT.zip here. After GitHub blob verification, the canonical source + manifest + governance will be rebound atomically.
